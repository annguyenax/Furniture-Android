# Android local config — mỗi máy tự đặt
**/local.properties

# Spring Boot local dev overrides — không commit (chứa credentials)
**/application-local.properties

# .env files — không commit (chứa credentials)
**/.env
!**/.env.example

# Local portable JDK
.jdk/
