<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".FurnitureApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FurnitureApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Splash Screen / Main Entry -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FurnitureApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <!-- Customer Activities -->
        <activity
            android:name=".ui.customer.CustomerMainActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <activity
            android:name=".ui.customer.product.ProductDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <activity
            android:name=".ui.customer.order.CheckoutActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <activity
            android:name=".ui.customer.order.OrderHistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <!-- Payment Deep Link -->
        <activity
            android:name=".ui.customer.order.PaymentCallbackActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="furnitureapp"
                    android:host="payment" />
            </intent-filter>
        </activity>

        <!-- Profile Activities -->
        <activity
            android:name=".ui.customer.profile.EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <!-- Shop Activities -->
        <activity
            android:name=".ui.customer.shop.ShopDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp" />

        <!-- Chat Activities -->
        <activity
            android:name=".ui.customer.chat.ChatActivity"
            android:exported="false"
            android:theme="@style/Theme.FurnitureApp"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>
